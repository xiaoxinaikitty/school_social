server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/school_social?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 20MB
  sql:
    init:
      mode: always

mybatis:
  mapper-locations: classpath:mapper/*Mapper.xml
  type-aliases-package: com.school.social.entity
  configuration:
    map-underscore-to-camel-case: true

app:
  mail:
    host: ${MAIL_HOST:smtp.qq.com}
    port: ${MAIL_PORT:587}
    username: ${MAIL_USERNAME:3268797894@qq.com}
    password: ${MAIL_PASSWORD:jkenyytvxzhrcicj}
    from: ${MAIL_FROM:3268797894@qq.com}
    use-ssl: ${MAIL_USE_SSL:true}
  auth:
    password-reset:
      code-expire-minutes: 10
      send-interval-seconds: 60
      max-send-per-hour: 10
      max-verify-attempts: 5
