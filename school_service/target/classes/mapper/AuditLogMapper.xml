<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.social.mapper.AuditLogMapper">
    <insert id="insert" parameterType="com.school.social.entity.AuditLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO audit_logs
        <trim prefix="(" suffix=")" suffixOverrides=",">
        <if test="targetType != null">target_type,</if>
        <if test="targetId != null">target_id,</if>
        <if test="auditorId != null">auditor_id,</if>
        <if test="decision != null">decision,</if>
        <if test="note != null">note,</if>
        <if test="createdAt != null">created_at,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
        <if test="targetType != null">#{targetType},</if>
        <if test="targetId != null">#{targetId},</if>
        <if test="auditorId != null">#{auditorId},</if>
        <if test="decision != null">#{decision},</if>
        <if test="note != null">#{note},</if>
        <if test="createdAt != null">#{createdAt},</if>
        </trim>
    </insert>
    <update id="updateById" parameterType="com.school.social.entity.AuditLog">
        UPDATE audit_logs
        <set>
        <if test="targetType != null">target_type = #{targetType},</if>
        <if test="targetId != null">target_id = #{targetId},</if>
        <if test="auditorId != null">auditor_id = #{auditorId},</if>
        <if test="decision != null">decision = #{decision},</if>
        <if test="note != null">note = #{note},</if>
        <if test="createdAt != null">created_at = #{createdAt},</if>
        </set>
        WHERE id = #{id}
    </update>
    <delete id="deleteById" parameterType="long">
        DELETE FROM audit_logs WHERE id = #{id}
    </delete>
    <select id="selectById" parameterType="long" resultType="com.school.social.entity.AuditLog">
        SELECT id, target_type, target_id, auditor_id, decision, note, created_at FROM audit_logs WHERE id = #{id}
    </select>
    <select id="selectAll" resultType="com.school.social.entity.AuditLog">
        SELECT id, target_type, target_id, auditor_id, decision, note, created_at FROM audit_logs
    </select>

</mapper>
