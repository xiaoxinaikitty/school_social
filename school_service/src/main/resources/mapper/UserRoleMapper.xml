<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.social.mapper.UserRoleMapper">
    <insert id="insert" parameterType="com.school.social.entity.UserRole">
        INSERT INTO user_roles
        <trim prefix="(" suffix=")" suffixOverrides=",">
        <if test="userId != null">user_id,</if>
        <if test="roleId != null">role_id,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
        <if test="userId != null">#{userId},</if>
        <if test="roleId != null">#{roleId},</if>
        </trim>
    </insert>
    <delete id="deleteByPk">
        DELETE FROM user_roles WHERE user_id = #{userId} AND role_id = #{roleId}
    </delete>
    <select id="selectByPk" resultType="com.school.social.entity.UserRole">
        SELECT user_id, role_id FROM user_roles WHERE user_id = #{userId} AND role_id = #{roleId}
    </select>
    <select id="selectAll" resultType="com.school.social.entity.UserRole">
        SELECT user_id, role_id FROM user_roles
    </select>

</mapper>
