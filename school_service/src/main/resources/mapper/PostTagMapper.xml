<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.school.social.mapper.PostTagMapper">
    <insert id="insert" parameterType="com.school.social.entity.PostTag">
        INSERT INTO post_tags
        <trim prefix="(" suffix=")" suffixOverrides=",">
        <if test="postId != null">post_id,</if>
        <if test="tagId != null">tag_id,</if>
        </trim>
        <trim prefix="VALUES (" suffix=")" suffixOverrides=",">
        <if test="postId != null">#{postId},</if>
        <if test="tagId != null">#{tagId},</if>
        </trim>
    </insert>
    <delete id="deleteByPk">
        DELETE FROM post_tags WHERE post_id = #{postId} AND tag_id = #{tagId}
    </delete>
    <select id="selectByPk" resultType="com.school.social.entity.PostTag">
        SELECT post_id, tag_id FROM post_tags WHERE post_id = #{postId} AND tag_id = #{tagId}
    </select>
    <select id="selectAll" resultType="com.school.social.entity.PostTag">
        SELECT post_id, tag_id FROM post_tags
    </select>
    <delete id="deleteByPostId">
        DELETE FROM post_tags WHERE post_id = #{postId}
    </delete>
    <select id="selectTagIdsByPostId" resultType="long">
        SELECT tag_id FROM post_tags WHERE post_id = #{postId}
    </select>

</mapper>
